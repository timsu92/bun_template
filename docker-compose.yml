services:
  app:
    environment:
      - PROJECT_PATH=/project/bun_template
      - TZ=Asia/Taipei
    build:
      context: .
      target: prod
      # secrets:
      #   - GITHUB_TOKEN
      args:
        - PROJECT_PATH=/project/bun_template
        - TZ=Asia/Taipei
    init: true
    # deploy:
    #   resources:
    #     reservations:
    #       devices:
    #         - driver: nvidia
    #           capabilities: [gpu]

# secrets:
#   GITHUB_TOKEN:
#     environment: GITHUB_TOKEN
